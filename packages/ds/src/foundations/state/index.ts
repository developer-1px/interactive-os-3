/**
 * state/ — interaction/selection mixin. token 없음.
 *   interactive.ts → mixin: hover(sel), focusRing(sel)…  — 인터랙션 셀렉터 주입.
 *   selection.ts   → mixin: selection(sel)               — 선택 시 색·강조.
 */
export * from './interactive'
export * from './selection'
