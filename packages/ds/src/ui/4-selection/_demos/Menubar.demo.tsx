import { Menubar } from '../Menubar'
import { Option } from '../Option'
export default () => (
  <Menubar>
    <Option>File</Option>
    <Option>Edit</Option>
    <Option>View</Option>
  </Menubar>
)
