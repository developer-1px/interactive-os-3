/**
 * breakpoint/ — viewport-scoped 토큰 (mobile/desktop 분기).
 *   shellMobileMax.ts → SHELL_MOBILE_MAX (mobile/desktop 경계)
 *   mobileGuide.ts    → mobileGuide, MobileGuideName (모바일 화면 layout guide outer margin spec)
 *
 * 산업 수렴 어휘: shellMobileMax(=mobile/desktop 경계). 추가 임계는 필요 시 등재.
 */
export * from './shellMobileMax'
export * from './mobileGuide'
