/**
 * SHELL_MOBILE_MAX — viewport 폭이 이 값 이하면 mobile shell 모드.
 *
 * `@media (max-width: ${SHELL_MOBILE_MAX})` 식으로 widget css`` 안에서 import 후 사용.
 * preset.shell.mobileMax 가 SSoT — preset 갈아끼우면 함께 변경.
 *
 * @demo type=value fn=SHELL_MOBILE_MAX
 */
export const SHELL_MOBILE_MAX = '600px'
