/**
 * @deprecated — breakpoint 은 별도 foundation lane (`semantic/breakpoint/`) 으로 격상됨.
 * 이 shim 은 기존 import 호환용. 신규 코드는 `from '@p/ds/tokens/semantic'` (barrel) 사용.
 */
export { SHELL_MOBILE_MAX } from '../breakpoint'
