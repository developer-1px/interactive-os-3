import { defineCategory } from "../../category-meta"
export default defineCategory({
  label: "Control",
  standard: "Material Interaction sizing",
  prefixes: ["--ds-control", "--ds-size", "--ds-chrome", "--ds-avatar", "--ds-touch-target"],
})
