import { defineCategory } from "../../category-meta"
export default defineCategory({
  label: "Sizing",
  standard: "Tailwind size scale · M3 box size · Polaris icon size",
  prefixes: ["--ds-size-"],
})
