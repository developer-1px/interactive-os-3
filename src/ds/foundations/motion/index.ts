/**
 * motion/ — 모션 스칼라 토큰. mixin 없음.
 *   duration.ts → token: dur('base') → 'var(--ds-dur-base)'
 *   easing.ts   → token: ease('out')  → 'var(--ds-ease-out)'
 */
export * from './duration'
export * from './easing'
